#🔮 ManagedBotUpdated

**This object represents a service message about a change of a managed bot's token.**

##⚙️ Properties

- **`managed_bot`** (**[User](User.md)** ): **The user that was created as a managed bot.**
- **`token`** (**`str`** ): **Optional. The new token of the managed bot.**
